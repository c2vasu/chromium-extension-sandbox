{
  "name": "chromium-extension-sandbox",
  "version": "1.1.0",
  "private": true,
  "description": "Replaces hashed Cloud Run IDs with their full namespaces in GCP Console.",
  "scripts": {
    "clean": "rimraf dist && mkdir -p dist",
    "copy": "cpy \"src/**\" \"!src/**/*.map\" dist",
    "build": "npm run clean && npm run copy",
    "zip": "node tools/zip.js",
    "dist": "npm run build && npm run zip"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "rimraf": "^5.0.0",
    "archiver": "^6.0.2"
  }
}
